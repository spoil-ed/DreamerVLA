# DreamerVLA WM-decoder 容量搜索 handoff — 2026-05-21

## TL;DR

目标：用**冻结的 pi0 SFT actor**（identity adapter + 原 output_projection），看能不能把 v4 系列 WM 的 hidden_rec 降下来 → 转化为 actor success。
当前最好：**ResNet L4×16384 wide**，hidden_rec=**0.074**，cos=**0.962**。刚跑完 diff diagnostic 验证 success rate，**结果未看**。

四个 v4-D 变体（14:59 启动）正在跑，其中 GPU 6 已挂。

---

## 1. 不变的约束（**严守**）

- **冻结 actor**：用户原话 "现在需要我们先用原actor就能完成，这样才是提升"
  - 必须用 `Pi0ActionHiddenActor(head_type=legacy, adapter_type=identity, freeze_output_projection=true)`
  - Actor finetuning（Path B）禁止
  - 所有提升必须来自 WM 侧（更好的 hidden 重建 → 更好的 imagine → 更好的 SFT actor 输入）
- **只能用 GPU 4-7**；`MUJOCO_GL=osmesa`；venv 同前
- 工作目录：`/home/user01/liops/workspace/DreamerVLA`

参考 memory：`project_dreamervla_architecture_correct`, `reference_dreamervla_paths`, `feedback_dreamervla_diagnostic_style`

---

## 2. 现在正在跑什么（4 个训练，1:59 PM 启动）

| GPU | 任务 | 配置 | 状态 |
|---|---|---|---|
| 4 | wm v4D resnet | ResMLPHead L4×u8192 | ✅ 跑（ep0 step 379, wm=703） |
| 5 | wm v4D pi0xform | Pi0StyleHiddenDecoder L4 d_model=1024 mem=8 | ✅ 跑（ep0 step 555, wm=254） |
| 6 | wm v4D v2mid | 沿用 v2-mid bottleneck | ❌ **挂了** ConnectionResetError，只剩 tee 孤儿 |
| 7 | wm v4D bigRSSM | deter=12288, stoch=48, classes=64 | ✅ 跑（ep0 step 657, wm=196） |

⚠️ **GPU 7 这次是 from-scratch 训 bigRSSM**（不像之前从 v3 ckpt init 导致 shape-mismatch），是 Q3 的干净对照。

📂 输出目录：`/home/user01/liops/workspace/DreamerVLA/data/outputs/dreamervla_diag/wm_pretrain_legacy_v4D_*_20260521_14594*`

---

## 3. 刚完成的后台任务

**diff diagnostic on ResNet wide best ckpt** — 完成（exit 0）但**结果未读**：
- 脚本：`scripts/wm_diff_v4/launch_diff_v4D_resnet_wide.sh`
- WM ckpt: `wm_pretrain_legacy_v4D_resnet_L4u16384_gpu6_20260521_153910/checkpoints/epoch=004-epoch_wm_loss=86.6151.ckpt`
- 测：`libero_goal task 0, 3 episodes, chunk_replay`
- 输出：`data/outputs/dreamervla_diag/action_diff_v4D_resnet_wide_L4u16384_e004_chunk_wm_gpu4_*/diff.log`

**下次开工第一件事：看这个 log。**

---

## 4. v4 系列变体最终成绩（hidden_rec，越低越好；目标 << 0.01）

| Variant | hidden_rec | 备注 |
|---|---|---|
| **ResNet L4×16384 wide** ⭐ | **0.074** | 当前最佳，正在 diff 验证 |
| ResNet L4×8192 | 0.109 | width > depth 验证 |
| V2-mid | 0.126 | encoder_hidden=2048 有 bottleneck |
| v4-B baseline | 0.131 | MLP L2×2048 |
| bigRSSM（旧的） | 0.148 | init 不公平，废 |
| Pi0xform resurrect | 0.168 | norm 匹配但 cos 差 |
| TSSM Token | 0.175 | KL 被 free_nats=1.0 卡住 |
| ResNet L6×8192 deep | 0.180 | 比 L4 差，**depth 没用** |
| Per-token MLP | **0.292** | 没学进去，35840 没空间冗余 |

历史最佳参考（**不同 obs_dim**）：`pi0_action_hidden_wm_20260512_tmux_resume1000_gpu4567` 在 **5120-dim** 上做到 0.0019。现在的 35840-dim 是 7× 更大的目标，norm ≈ √35840 ≈ 189。

---

## 5. 用户的三个未答问题

### Q1：之前所有效果是不是训到平台期才停？
**部分是。** Best-ckpt loss 在最后 3-4 ckpt 间差值 < 0.001（已平台），但 **per_token_mlp 根本没学**（损失没降，可能架构 bug）。结论：**结构/容量是瓶颈，不是训练步数**。

### Q2：TSSM (TransDreamer) flat 复现如何？
**没跑成功过。** dtype bug（pos_emb f32 vs 模型 bf16）修完后被用户引导到 Token 版本，flat 版从未完成一次训练。**应补跑**。
代码：`src/models/world_model/tssm_torch.py::TSSMRynnBackboneWorldModel`

### Q3：deter/stoch 维度太低？
**当前 GPU 7 正在 from-scratch 训 bigRSSM** (deter=12288, stoch=48×64, feat≈15360)。需要等几小时跑到 plateau 才能答。

---

## 6. 工程化重构（**已完成，已测试**）

`src/models/world_model/` 重组成 diffusion_policy 风格：
- `common.py`：模型无关原语（RMSNorm, MLPHead, ResMLPHead, BlockLinear, act）
- `dreamerv3_torch.py`：DreamerV3/RSSM 全族（RSSM, encoder/decoder, hidden_decoders, heads, WM）
- `tssm_torch.py`：TSSM 全族（transformer cell, TSSMDynamic, TSSMTokenDynamic, WM）
- `STRUCTURE.md`：完整文档

**两种 cfg 风格都支持**：
- (A) `hidden_decoder_kind: per_token_mlp` 等字符串 dispatch（向后兼容）
- (B) `hidden_decoder: {_target_: ...}` LEGO 组合（推荐新用）

验证等价：`OLD kind-string: 196.8M / NEW _target_: 196.8M ✓`

示例 cfg：`configs/example_lego_per_token_mlp.yaml`
变体索引：`scripts/wm_variants_v4_v4E/README.md`（16 个 launch 脚本）

---

## 7. 关键文件

| 路径 | 用途 |
|---|---|
| `src/models/world_model/common.py` | 模型无关原语 |
| `src/models/world_model/dreamerv3_torch.py` | DreamerV3 全族；含新的 `PerTokenMLPHead`, `Pi0StyleHiddenDecoder`, `ResMLPHead` |
| `src/models/world_model/tssm_torch.py` | TSSM 全族（含 flat 和 Token 两套） |
| `src/models/world_model/STRUCTURE.md` | 工程结构说明 |
| `scripts/wm_variants_v4_v4E/` | 16 个 v4 launch 脚本 + README |
| `scripts/wm_diff_v4/` | diff diagnostic 脚本 |
| `configs/dreamer_vla_libero_goal_pi0_legacy_action_hidden_head_actor_v4d_*.yaml` | v4-D 各变体 cfg |
| `configs/example_lego_per_token_mlp.yaml` | LEGO 风格 cfg 范例 |

---

## 8. 下一步候选（按优先级）

1. **看 diff log**：`data/outputs/dreamervla_diag/action_diff_v4D_resnet_wide_L4u16384_e004_chunk_wm_gpu4_*/diff.log`
   - 若 0.074 hidden_rec 没换成 actor success → 说明 hidden_rec 不是正确指标，需要换 metric
   - 若有 success → ResNet wide 路线 work，可以再加宽试 L4×32768

2. **救 GPU 6**：起一个 flat TSSM (`TSSMRynnBackboneWorldModel`)，补 Q2 缺口
   - 用 `_target_: src.models.world_model.tssm_torch.TSSMRynnBackboneWorldModel`
   - 注意 bf16 cast 已修，可以直接跑

3. **等 GPU 7 bigRSSM 训完**（几小时）→ 答 Q3
   - 如果 bigRSSM hidden_rec < ResNet wide → latent 容量是瓶颈
   - 否则 → decoder 容量才是瓶颈，继续加宽 decoder

4. **清 tmux**：12 个 session 里 10 个是 4-5 月的死壳，可以 `tmux kill-session -t <name>` 清掉降噪

---

## 9. shell / 进程现状

```
tmux ls:        12 个 session（10 个远古死壳，2 个较新）
活的训练:        4 个 python 主进程（GPU 4,5,7 + GPU 6 已死）
GPU 4-7 占用:    4: 21G ✓ | 5: 仍在 init / 跑中 | 6: 8MiB ✗ | 7: 跑中
```

排查命令：
```bash
tmux ls
nvidia-smi --query-gpu=index,memory.used,utilization.gpu --format=csv,noheader -i 4,5,6,7
ps -ef | grep train.py | grep dreamervla_diag | grep -v grep
tail -n 30 data/outputs/dreamervla_diag/wm_pretrain_legacy_v4D_*/wm_pretrain.log
```
