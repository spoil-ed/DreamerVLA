# @package _global_
defaults:
  - openvla_oft
  - _self_

training:
  out_dir: ${oc.env:OUT_DIR,${oc.env:DVLA_DATA_ROOT,${oc.env:DVLA_ROOT,.}/data}/outputs/vla/openvla_oft_lm_head_one_trajectory/${task.name}/${now:%Y%m%d_%H%M%S}}

dataset:
  max_demos_per_file: null
  demos_per_task: 1
  demo_selection_seed: ${seed}
