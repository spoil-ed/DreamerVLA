defaults:
  - _base_libero
  - _self_

name: libero_object
suite: libero_object
task_name: object

action_horizon: 8
time_horizon: 8

openvla_oft:
  ckpt_path: ${oc.env:DVLA_DATA_ROOT,${oc.env:DVLA_ROOT,.}/data}/checkpoints/Openvla-oft-SFT-traj1/Openvla-oft-SFT-libero-object-traj1
  dataset_statistics_path: ${task.openvla_oft.ckpt_path}/dataset_statistics.json
  failure_hdf5_dir: null
