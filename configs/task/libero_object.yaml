defaults:
  - _base_libero
  - _self_

name: libero_object
suite: libero_object
task_name: object

action_horizon: 5
time_horizon: 5

legacy_action_hidden:
  wm_obs_dim: 35840
  token_count: 35
  chunk_size: 5
  latent_source: RynnVLA legacy [5,7,1024] action hidden

openvla_oft:
  ckpt_path: ${oc.env:DVLA_DATA_ROOT,${oc.env:DVLA_ROOT,.}/data}/checkpoints/OpenVLA-OFT/libero_object
  component_ckpt_dir: ${oc.env:DVLA_DATA_ROOT,${oc.env:DVLA_ROOT,.}/data}/checkpoints/OpenVLA-OFT/libero_object
  resume_step: 150000
  dataset_statistics_path: ${oc.env:DVLA_DATA_ROOT,${oc.env:DVLA_ROOT,.}/data}/checkpoints/OpenVLA-OFT/libero_object/dataset_statistics.json
  action_head_ckpt: ${oc.env:DVLA_DATA_ROOT,${oc.env:DVLA_ROOT,.}/data}/checkpoints/OpenVLA-OFT/libero_object/action_head--150000_checkpoint.pt
  proprio_projector_ckpt: ${oc.env:DVLA_DATA_ROOT,${oc.env:DVLA_ROOT,.}/data}/checkpoints/OpenVLA-OFT/libero_object/proprio_projector--150000_checkpoint.pt
  failure_hdf5_dir: null
