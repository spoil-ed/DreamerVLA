# @package _global_
defaults:
  - /task: openvla_onetraj_coldstart_libero
  - override /dreamervla: openvla_onetraj_libero_cotrain_ray
  - _self_

runner:
  logger:
    logger_backends: [tensorboard, wandb]
    wandb_mode: offline
    wandb_proxy: null
