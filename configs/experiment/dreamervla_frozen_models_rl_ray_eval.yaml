# @package _global_
defaults:
  - dreamervla_frozen_models_rl_ray
  - _self_

# True real-LIBERO evaluation is outside imagined replay/PPO: the immutable
# base VLA at step 0, then the current VLA-hidden Actor policy every ten steps.
manual_cotrain:
  eval_interval_global_steps: 10
  eval_initial_global_step: true
