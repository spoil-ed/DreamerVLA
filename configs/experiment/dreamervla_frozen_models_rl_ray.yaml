# @package _global_
defaults:
  - /task: openvla_onetraj_libero
  - override /dreamervla: frozen_models_rl_ray
  - override /classifier: openvla_oft_spatial
  - override /pre_mainline: libero_goal_official
  - _self_

runner:
  logger:
    logger_backends: [tensorboard, wandb]
    wandb_mode: offline
    wandb_proxy: null
