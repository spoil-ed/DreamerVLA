# @package _global_
defaults:
  - openvla_oft_hdf5
  - _self_

training:
  out_dir: ${oc.env:OUT_DIR,${oc.env:DVLA_DATA_ROOT,data}/outputs/vla/openvla_oft_l1_one_trajectory/${task.name}/${now:%Y%m%d_%H%M%S}}

dataset:
  max_demos_per_file: null
  demos_per_task: 1
  demo_selection_seed: ${seed}
