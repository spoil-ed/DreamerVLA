# Core DreamerVLA runtime dependencies.
# Keep this list curated: do not mirror a developer machine's full pip list.
# PyTorch is installed separately in scripts/install/20_torch.sh.

diffusers==0.33.0
bddl==1.0.1
dill
draccus==0.8.0
easydict
gym==0.25.2
h5py
hydra-core
imageio[ffmpeg]
matplotlib
mujoco
numpy==1.26.4
omegaconf
opencv-python
packaging
pandas
peft==0.11.0
protobuf
psutil
PyOpenGL==3.1.9
PyOpenGL_accelerate==3.1.9
pybind11
scikit-learn
scipy
sentencepiece==0.1.99
tensorboard
timm==0.9.10
tokenizers==0.19.1
wandb

# Build/runtime helpers used by local third_party installs and optional kernels.
cython==0.29.36
ninja
numba

# Installed separately by the installer when enabled.
# flash-attn==2.5.5
